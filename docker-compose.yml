services:
  postgres:
    image: postgres:17
    container_name: order-postgres
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: orderdb
      POSTGRES_USER: order
      POSTGRES_PASSWORD: order1234
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
